# Utils 模块说明

- **jwt.rs**：封装 JWT 生成/验证逻辑，默认从 `JWT_SECRET` 环境变量读取密钥，仅在开发场景使用回退默认值。
- **http.rs**：HTTP Header 相关工具，例如从 `Authorization` 中提取用户 ID；示例代码位于文档注释中。
- **tool.rs**：通用工具集合，目前主要提供密码哈希与校验（bcrypt 封装）。

> 提示：在引入新的工具类时，请优先确定其职责是否属于核心业务；若涉及外部依赖并与具体技术栈绑定，应考虑放入 `adapters`。
