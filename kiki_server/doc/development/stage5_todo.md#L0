# 阶段5待办清单（文档 & 注释补全）

## 需要补充 README / 注释的目录
- ✅ `src/core`：说明实体、用例、端口的职责及依赖规则。
- ✅ `src/adapters`：拆分 HTTP / Persistence 说明以及如何实现新的适配器。
- ✅ `src/framework/bootstrap`：描述启动流程、依赖注入、路由装配步骤。
- ✅ `src/utils`：在 JWT / HTTP 工具处补充使用示例和安全提示。

## 兼容层清理计划
- ✅ `src/application/use_cases/mod.rs`：兼容导出已删除，所有调用改为直接引用 `core::use_cases`。
- ✅ `src/infrastructure`：日志实现迁移至 `framework::logging`，兼容目录已移除。
- ✅ `src/presentation/http/mod.rs`：HTTP 兼容导出已下线，统一使用 `adapters::http`。

## 文档同步项
- ✅ 更新 `doc/framework/latest_ddd_architecture_2025.md` 以匹配新目录结构。
- ✅ 在 `doc/features/*` 中标注历史实现已归档，避免混淆。
- ✅ 追加 `README` 或在 `doc/development/architecture_current.md` 中记录 Stage5 完成标准。

## 校验任务
- `cargo fmt`（若后续增量注释导致格式变化）。
- `cargo clippy`（可选，确保新注释与导出符合规范）。
- 保持 `cargo test`、`cargo check` 通过。
