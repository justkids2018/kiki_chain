name: Docker Build and Deploy

on:
  push:
    branches:
      - "main"
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag override (default: sha-<commit>)"
        required: false
        type: string
      skip_db_migration:
        description: "Skip database migration (only update app)"
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_BACKEND: ghcr.io/justkids2018/kiki-chain-backend
  IMAGE_ADMIN: ghcr.io/justkids2018/kiki-chain-admin

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.resolve_tag.outputs.image_tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve image tag
        id: resolve_tag
        shell: bash
        run: |
          if [[ -n "${{ github.event.inputs.image_tag }}" ]]; then
            TAG="${{ github.event.inputs.image_tag }}"
          else
            TAG="sha-${GITHUB_SHA::8}"
          fi
          echo "IMAGE_TAG=$TAG" >> "$GITHUB_ENV"
          echo "image_tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./kiki_server
          file: ./kiki_server/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_BACKEND }}:${{ env.IMAGE_TAG }}
            ${{ env.IMAGE_BACKEND }}:latest
          cache-from: type=registry,ref=${{ env.IMAGE_BACKEND }}:latest
          cache-to: type=inline

      - name: Build and push admin
        uses: docker/build-push-action@v6
        with:
          context: ./kiki_admin
          file: ./kiki_admin/Dockerfile
          push: true
          build-args: |
            VITE_API_BASE_URL=https://kiki.keepthinking.me
          tags: |
            ${{ env.IMAGE_ADMIN }}:${{ env.IMAGE_TAG }}
            ${{ env.IMAGE_ADMIN }}:latest
          cache-from: type=registry,ref=${{ env.IMAGE_ADMIN }}:latest
          cache-to: type=inline

      - name: Build summary
        run: |
          echo "### 🐳 Docker Images Built" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Service | Image | Tag |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|-------|-----|" >> $GITHUB_STEP_SUMMARY
          echo "| Backend | \`${{ env.IMAGE_BACKEND }}\` | \`${{ env.IMAGE_TAG }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Admin | \`${{ env.IMAGE_ADMIN }}\` | \`${{ env.IMAGE_TAG }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY

  deploy-tencent:
    runs-on: ubuntu-latest
    needs: build-and-push
    if: ${{ github.event_name != 'pull_request' }}
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate deploy prerequisites
        env:
          TENCENT_SSH_PRIVATE_KEY: ${{ secrets.TENCENT_SSH_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          if [[ -z "${TENCENT_SSH_PRIVATE_KEY}" ]]; then
            echo "Missing required secret: TENCENT_SSH_PRIVATE_KEY"
            echo "Please add your Tencent server SSH private key to repository secrets."
            exit 1
          fi
          if [[ ! -f kiki_server/.env ]]; then
            echo "Missing kiki_server/.env in checkout."
            echo "Either commit the backend env file or provide DEPLOY_QINIU_* via workflow env/secrets."
            exit 1
          fi
          if [[ ! -f kiki_server/database/init.sql ]]; then
            echo "Missing kiki_server/database/init.sql in checkout."
            echo "Database initialization SQL must be committed with the backend database fact source."
            exit 1
          fi
          if [[ ! -d kiki_server/database/migrations ]]; then
            echo "Missing kiki_server/database/migrations in checkout."
            echo "Database migrations must live under kiki_server/database/migrations."
            exit 1
          fi

      - name: Setup SSH
        env:
          TENCENT_SSH_PRIVATE_KEY: ${{ secrets.TENCENT_SSH_PRIVATE_KEY }}
          DEPLOY_SERVER_IP: ${{ secrets.DEPLOY_SERVER_IP }}
        run: |
          set -euo pipefail
          # 清理可能的空格和换行符
          SERVER_IP=$(echo "${DEPLOY_SERVER_IP}" | tr -d '[:space:]')
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf "%s\n" "${TENCENT_SSH_PRIVATE_KEY}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H "${SERVER_IP}" >> ~/.ssh/known_hosts
          chmod 644 ~/.ssh/known_hosts

      - name: Remote login to GHCR (optional)
        env:
          GHCR_USERNAME: ${{ secrets.GHCR_USERNAME }}
          GHCR_READ_TOKEN: ${{ secrets.GHCR_READ_TOKEN }}
          DEPLOY_SSH_USER: ${{ secrets.DEPLOY_SSH_USER }}
          DEPLOY_SERVER_IP: ${{ secrets.DEPLOY_SERVER_IP }}
        run: |
          set -euo pipefail
          # 清理可能的空格和换行符
          SERVER_IP=$(echo "${DEPLOY_SERVER_IP}" | tr -d '[:space:]')
          SSH_USER=$(echo "${DEPLOY_SSH_USER}" | tr -d '[:space:]')
          if [[ -n "${GHCR_USERNAME}" && -n "${GHCR_READ_TOKEN}" ]]; then
            echo "Attempting remote GHCR login..."
            if ssh "${SSH_USER}@${SERVER_IP}" "echo '${GHCR_READ_TOKEN}' | docker login ghcr.io -u '${GHCR_USERNAME}' --password-stdin" 2>&1; then
              echo "✅ Remote GHCR login successful."
            else
              echo "⚠️ Remote GHCR login failed. Images are public, continuing anyway..."
            fi
          else
            echo "GHCR_USERNAME/GHCR_READ_TOKEN not set; skip remote GHCR login."
          fi

      - name: Generate deploy artifacts (step1)
        env:
          DEPLOY_IMAGE_TAG: ${{ needs.build-and-push.outputs.image_tag }}
          DEPLOY_SERVER_IP: ${{ secrets.DEPLOY_SERVER_IP }}
          DEPLOY_SSH_USER: ${{ secrets.DEPLOY_SSH_USER }}
          DEPLOY_REMOTE_DIR: ${{ secrets.DEPLOY_REMOTE_DIR }}
          DEPLOY_ADMIN_HOST_PORT: ${{ secrets.DEPLOY_ADMIN_HOST_PORT }}
          DEPLOY_BACKEND_HOST_PORT: ${{ secrets.DEPLOY_BACKEND_HOST_PORT }}
          DEPLOY_POSTGRES_HOST_PORT: ${{ secrets.DEPLOY_POSTGRES_HOST_PORT }}
        run: |
          set -euo pipefail
          ./scripts/deploy-release/step1-prepare.sh tencent

      - name: Deploy to Tencent server (step2)
        env:
          DEPLOY_SERVER_IP: ${{ secrets.DEPLOY_SERVER_IP }}
          DEPLOY_SSH_USER: ${{ secrets.DEPLOY_SSH_USER }}
          DEPLOY_REMOTE_DIR: ${{ secrets.DEPLOY_REMOTE_DIR }}
          DEPLOY_ADMIN_HOST_PORT: ${{ secrets.DEPLOY_ADMIN_HOST_PORT }}
          DEPLOY_BACKEND_HOST_PORT: ${{ secrets.DEPLOY_BACKEND_HOST_PORT }}
          DEPLOY_POSTGRES_HOST_PORT: ${{ secrets.DEPLOY_POSTGRES_HOST_PORT }}
          SKIP_DB_MIGRATION: ${{ github.event.inputs.skip_db_migration == true && 'true' || 'false' }}
        run: |
          set -euo pipefail
          ./scripts/deploy-release/step2-deploy.sh tencent

      - name: Deployment summary
        if: always()
        run: |
          echo "### 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Item | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Server | \`${{ secrets.DEPLOY_SERVER_IP }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| Image Tag | \`${{ needs.build-and-push.outputs.image_tag }}\` |" >> $GITHUB_STEP_SUMMARY
          echo "| DB Migration | ${{ github.event.inputs.skip_db_migration == true && '⏭️ Skipped' || '✅ Executed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Status | ${{ job.status == 'success' && '✅ Success' || '❌ Failed' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**URLs**:" >> $GITHUB_STEP_SUMMARY
          echo "- Admin: https://admin.keepthinking.me" >> $GITHUB_STEP_SUMMARY
          echo "- API: https://kiki.keepthinking.me/api/" >> $GITHUB_STEP_SUMMARY
          echo "- Main: https://kiki.keepthinking.me" >> $GITHUB_STEP_SUMMARY
