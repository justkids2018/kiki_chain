# 奇奇漫游记 - 开发环境配置
# 用于本地开发调试，优化开发体验
# 注意：日志配置独立处理，通过环境变量 LOG_LEVEL 和 LOG_COLORS 控制

# ==========================================
# 服务器配置
# ==========================================
[server]
# Docker 环境需要绑定 0.0.0.0 才能从容器外部访问
host = "0.0.0.0"
port = 8081

# 开发环境CORS配置：包含常用开发端口与通配符，方便各种调试工具直连
cors_origins = [
    "*",                          # 允许所有源（开发环境专用）
    "http://localhost:5173",      # Vite默认端口
    "http://localhost:5176",      # kiki_admin 端口
    "http://localhost:5178",      # kiki_admin 备用端口
    "http://127.0.0.1:5178",      # kiki_admin 备用端口（回环）
    "http://127.0.0.1:3000",      # API服务端口
    "http://localhost:3000",      # API服务端口
    "http://127.0.0.1:3000",      # 本地回环地址
    "http://127.0.0.1:5173",      # 本地回环地址（Vite）
    "http://localhost:4173",      # Vite预览端口
    "http://localhost:3001"       # 备用前端端口
]

# ==========================================
# 安全配置
# ==========================================
[security]
# 开发环境JWT过期时间更长，减少重复登录
jwt_expiration_hours = 72
