# 登录功能 - 修复实施计划

**创建时间**: 2026-01-22
**基于报告**: 登录功能需求验证报告.md
**总问题数**: 3个Critical, 2个Medium, 2个Low
**预计总时间**: 17小时（Phase 1+2）

---

## 📊 总览

| 阶段 | 问题数 | 预计时间 | 优先级 | 状态 |
|------|--------|---------|--------|------|
| Phase 1: Critical | 3 | 6.5h | P0 | 🔴 待开始 |
| Phase 2: Medium | 2 | 9h | P1 | ⏳ 等待Phase 1 |
| Phase 3: Low | 2 | 1.5h | P2 | ⏳ 可延后 |

---

## 🔴 Phase 1: Critical Issues (必须修复)

### Task 1: 修复User数据模型不一致

**优先级**: P0
**预计时间**: 2小时
**状态**: 🔴 Not Started

**问题描述**:
API返回的User字段与前端实体不匹配，导致用户信息显示错误。

**影响文件**:
- `lib/domain/entities/user.dart`
- `lib/data/repositories/auth_repository_impl.dart`
- `lib/presentation/controllers/auth_controller.dart`
- `lib/presentation/widgets/profile_tab.dart` (未来使用)

**修改详情**:

#### 1.1 修改User实体 (`user.dart`)
```dart
// 修改前（错误）
class User {
  final int id;           // ❌ 应该是String
  final String uid;       // ❌ API没有此字段
  final String name;      // ❌ API字段是nickname
  final String email;     // ❌ API没有此字段
  final String phone;
  final DateTime createdAt;
  final DateTime updatedAt;  // ❌ API字段是lastLoginAt
  final int roleId;       // ❌ API没有此字段
}

// 修改后（正确）
class User {
  final String id;              // ✅ 改为String
  final String phone;
  final String nickname;        // ✅ 改名
  final String? avatar;         // ✅ 新增
  final DateTime createdAt;
  final DateTime lastLoginAt;   // ✅ 改名
}
```

#### 1.2 更新Repository映射 (`auth_repository_impl.dart`)
```dart
// 修改login方法中的User创建代码
final user = User(
  id: userData['id'] as String,      // ✅ 不再转换为int
  phone: userData['phone'] ?? '',
  nickname: userData['nickname'] ?? '',  // ✅ 使用nickname
  avatar: userData['avatar'],        // ✅ 新增
  createdAt: userData['createdAt'] != null
      ? DateTime.parse(userData['createdAt'])
      : DateTime.now(),
  lastLoginAt: userData['lastLoginAt'] != null  // ✅ 使用lastLoginAt
      ? DateTime.parse(userData['lastLoginAt'])
      : DateTime.now(),
);
```

#### 1.3 更新AuthController (`auth_controller.dart`)
```dart
// 修改相关getter
// 删除 getRoleName、isTeacher、isStudent、isAdmin 方法
// 因为roleId字段已删除
```

**测试检查点**:
- [ ] User.fromJson()解析API响应正确
- [ ] user.nickname显示正确
- [ ] user.avatar可为null
- [ ] user.lastLoginAt时间正确
- [ ] 旧代码中没有使用user.uid、user.email、user.roleId

**依赖**: 无

---

### Task 2: 密码加密提前到Controller层

**优先级**: P0
**预计时间**: 30分钟
**状态**: 🔴 Not Started

**问题描述**:
密码从Controller传到Repository时仍是明文，存在安全风险。

**影响文件**:
- `lib/presentation/controllers/auth_controller.dart`
- `lib/domain/repositories/i_auth_repository.dart`

**修改详情**:

#### 2.1 修改AuthController (`auth_controller.dart`)
```dart
// login方法修改
Future<bool> login() async {
  if (!loginFormKey.currentState!.validate()) {
    return false;
  }

  try {
    EasyLoading.show(status: '登录中...');

    final identifier = loginIdentifierController.text.trim();

    // ✅ 立即加密，不传递明文
    final encryptedPassword = CryptoUtils.sha256Encrypt(loginPasswordController.text);

    // ✅ 传递加密后的密码
    final user = await _authRepository.login(identifier, encryptedPassword);

    // ... 后续逻辑不变
  }
}

// register方法同样修改
Future<bool> register() async {
  // ... 验证逻辑

  final phone = registerPhoneController.text.trim();

  // ✅ 立即加密
  final encryptedPassword = CryptoUtils.sha256Encrypt(registerPasswordController.text);
  final nickname = registerNicknameController.text.trim();

  // ✅ 传递加密后的密码
  final user = await _authRepository.register(
    phone,
    encryptedPassword,
    nickname: nickname.isEmpty ? null : nickname,
  );

  // ... 后续逻辑不变
}
```

#### 2.2 更新Repository接口注释 (`i_auth_repository.dart`)
```dart
/// 用户登录
///
/// 参数:
/// - [phone] 手机号（11位数字）
/// - [encryptedPassword] 密码（已SHA256加密）✅ 明确标注
Future<User?> login(String phone, String encryptedPassword);

/// 用户注册
///
/// 参数:
/// - [phone] 手机号（11位数字）
/// - [encryptedPassword] 密码（已SHA256加密）✅ 明确标注
/// - [nickname] 昵称（可选，2-20字符）
Future<User?> register(String phone, String encryptedPassword, {String? nickname});
```

#### 2.3 删除Repository实现中的重复加密 (`auth_repository_impl.dart`)
```dart
@override
Future<User?> login(String phone, String encryptedPassword) async {
  try {
    AppLogger.info('🔐 尝试登录: $phone');

    // ❌ 删除这行：final encryptedPassword = CryptoUtils.sha256Encrypt(password);
    // ✅ 直接使用传入的encryptedPassword

    final response = await _requestManager.post<Map<String, dynamic>>(
      ApiEndpoints.authLogin,
      data: {
        'phone': phone,
        'password': encryptedPassword,  // ✅ 已经是加密后的
      },
    );

    // ... 后续逻辑不变
  }
}

// register方法同样修改
```

**测试检查点**:
- [ ] 登录时密码立即加密
- [ ] 注册时密码立即加密
- [ ] Repository不再进行二次加密
- [ ] 网络请求中密码为SHA256值

**依赖**: 无

---

### Task 3: 实现"我的"Tab页面

**优先级**: P0
**预计时间**: 4小时
**状态**: 🔴 Not Started

**问题描述**:
缺少"我的"Tab页面，用户无法查看个人信息和退出登录。

**影响文件**:
- `lib/presentation/pages/profile/profile_tab.dart` (新建)
- `lib/presentation/pages/profile/widgets/guest_profile.dart` (新建)
- `lib/presentation/pages/profile/widgets/logged_in_profile.dart` (新建)
- `lib/config/app_routes.dart` (可能需要)
- `lib/presentation/widgets/bottom_navigation.dart` (如果有)

**修改详情**:

#### 3.1 创建ProfileTab主页面 (`profile_tab.dart`)
```dart
import 'package:flutter/material.dart';
import 'package:get/get.dart';
import '../../controllers/auth_controller.dart';
import 'widgets/guest_profile.dart';
import 'widgets/logged_in_profile.dart';

/// "我的"Tab页面
///
/// 根据登录状态显示不同内容：
/// - 未登录：显示GuestProfile
/// - 已登录：显示LoggedInProfile
class ProfileTab extends StatelessWidget {
  const ProfileTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final AuthController authController = Get.find<AuthController>();

    return Scaffold(
      backgroundColor: Color(0xFFF8FAFC),
      body: SafeArea(
        child: Obx(() {
          if (authController.isLoggedIn && authController.currentUser != null) {
            return LoggedInProfile(user: authController.currentUser!);
          } else {
            return const GuestProfile();
          }
        }),
      ),
    );
  }
}
```

#### 3.2 创建游客模式UI (`guest_profile.dart`)
```dart
import 'package:flutter/material.dart';
import 'package:get/get.dart';

/// 未登录状态的Profile界面
class GuestProfile extends StatelessWidget {
  const GuestProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        padding: EdgeInsets.all(32),
        constraints: BoxConstraints(maxWidth: 400),
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            // 默认头像
            Container(
              width: 80,
              height: 80,
              decoration: BoxDecoration(
                color: Color(0xFFE2E8F0),
                shape: BoxShape.circle,
              ),
              child: Icon(
                Icons.person_outline,
                size: 40,
                color: Color(0xFF6B7280),
              ),
            ),

            SizedBox(height: 24),

            // 提示文字
            Text(
              'Hi，快来登录吧',
              style: TextStyle(
                fontSize: 20,
                fontWeight: FontWeight.w600,
                color: Color(0xFF27273F),
              ),
            ),

            SizedBox(height: 32),

            // 登录按钮
            SizedBox(
              width: double.infinity,
              height: 50,
              child: ElevatedButton(
                onPressed: () => Get.toNamed('/login'),
                style: ElevatedButton.styleFrom(
                  backgroundColor: Color(0xFF00C37D),
                  foregroundColor: Colors.white,
                  shape: RoundedRectangleBorder(
                    borderRadius: BorderRadius.circular(12),
                  ),
                ),
                child: Text(
                  '登录',
                  style: TextStyle(
                    fontSize: 16,
                    fontWeight: FontWeight.w600,
                  ),
                ),
              ),
            ),

            SizedBox(height: 16),

            // 注册按钮
            SizedBox(
              width: double.infinity,
              height: 50,
              child: OutlinedButton(
                onPressed: () => Get.toNamed('/register'),
                style: OutlinedButton.styleFrom(
                  foregroundColor: Color(0xFF00C37D),
                  side: BorderSide(color: Color(0xFF00C37D), width: 2),
                  shape: RoundedRectangleBorder(
                    borderRadius: BorderRadius.circular(12),
                  ),
                ),
                child: Text(
                  '注册',
                  style: TextStyle(
                    fontSize: 16,
                    fontWeight: FontWeight.w600,
                  ),
                ),
              ),
            ),
          ],
        ),
      ),
    );
  }
}
```

#### 3.3 创建已登录UI (`logged_in_profile.dart`)
```dart
import 'package:flutter/material.dart';
import 'package:get/get.dart';
import '../../../domain/entities/user.dart';
import '../../controllers/auth_controller.dart';

/// 已登录状态的Profile界面
class LoggedInProfile extends StatelessWidget {
  final User user;

  const LoggedInProfile({Key? key, required this.user}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    final AuthController authController = Get.find<AuthController>();

    return SingleChildScrollView(
      padding: EdgeInsets.all(24),
      child: Column(
        children: [
          // 用户信息卡片
          _buildUserInfoCard(),

          SizedBox(height: 24),

          // 菜单列表
          _buildMenuList(authController),
        ],
      ),
    );
  }

  Widget _buildUserInfoCard() {
    return Container(
      padding: EdgeInsets.all(24),
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(16),
        border: Border.all(color: Color(0xFFE2E8F0), width: 1),
      ),
      child: Row(
        children: [
          // 头像
          Container(
            width: 64,
            height: 64,
            decoration: BoxDecoration(
              color: user.avatar != null ? null : Color(0xFFE2E8F0),
              shape: BoxShape.circle,
              image: user.avatar != null
                  ? DecorationImage(
                      image: NetworkImage(user.avatar!),
                      fit: BoxFit.cover,
                    )
                  : null,
            ),
            child: user.avatar == null
                ? Icon(Icons.person, size: 32, color: Color(0xFF6B7280))
                : null,
          ),

          SizedBox(width: 16),

          // 用户信息
          Expanded(
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text(
                  user.nickname,
                  style: TextStyle(
                    fontSize: 18,
                    fontWeight: FontWeight.w600,
                    color: Color(0xFF27273F),
                  ),
                ),
                SizedBox(height: 4),
                Text(
                  '手机号：${_maskPhone(user.phone)}',
                  style: TextStyle(
                    fontSize: 14,
                    color: Color(0xFF6B7280),
                  ),
                ),
              ],
            ),
          ),
        ],
      ),
    );
  }

  Widget _buildMenuList(AuthController authController) {
    return Container(
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(16),
        border: Border.all(color: Color(0xFFE2E8F0), width: 1),
      ),
      child: Column(
        children: [
          _buildMenuItem(
            icon: Icons.bar_chart_rounded,
            title: '学习记录',
            onTap: () {
              // TODO: 导航到学习记录页
              Get.snackbar('提示', '学习记录功能开发中');
            },
          ),
          Divider(height: 1),
          _buildMenuItem(
            icon: Icons.star_outline_rounded,
            title: '我的收藏',
            onTap: () {
              // TODO: 导航到收藏页
              Get.snackbar('提示', '收藏功能开发中');
            },
          ),
          Divider(height: 1),
          _buildMenuItem(
            icon: Icons.settings_outlined,
            title: '设置',
            onTap: () {
              // TODO: 导航到设置页
              Get.snackbar('提示', '设置功能开发中');
            },
          ),
          Divider(height: 1),
          _buildMenuItem(
            icon: Icons.logout_rounded,
            title: '退出登录',
            textColor: Color(0xFFEF4444),
            showArrow: false,
            onTap: () async {
              // 确认弹窗
              final shouldLogout = await Get.dialog<bool>(
                AlertDialog(
                  title: Text('退出登录'),
                  content: Text('确定要退出登录吗？'),
                  actions: [
                    TextButton(
                      onPressed: () => Get.back(result: false),
                      child: Text('取消'),
                    ),
                    ElevatedButton(
                      onPressed: () => Get.back(result: true),
                      style: ElevatedButton.styleFrom(
                        backgroundColor: Color(0xFFEF4444),
                      ),
                      child: Text('退出'),
                    ),
                  ],
                ),
              );

              if (shouldLogout == true) {
                await authController.logout();
              }
            },
          ),
        ],
      ),
    );
  }

  Widget _buildMenuItem({
    required IconData icon,
    required String title,
    required VoidCallback onTap,
    Color? textColor,
    bool showArrow = true,
  }) {
    return InkWell(
      onTap: onTap,
      child: Padding(
        padding: EdgeInsets.symmetric(horizontal: 20, vertical: 16),
        child: Row(
          children: [
            Icon(icon, color: textColor ?? Color(0xFF27273F), size: 24),
            SizedBox(width: 12),
            Expanded(
              child: Text(
                title,
                style: TextStyle(
                  fontSize: 16,
                  fontWeight: FontWeight.w500,
                  color: textColor ?? Color(0xFF27273F),
                ),
              ),
            ),
            if (showArrow)
              Icon(Icons.chevron_right, color: Color(0xFF9CA3AF), size: 24),
          ],
        ),
      ),
    );
  }

  String _maskPhone(String phone) {
    if (phone.length == 11) {
      return '${phone.substring(0, 3)}****${phone.substring(7)}';
    }
    return phone;
  }
}
```

**测试检查点**:
- [ ] 未登录时显示游客界面
- [ ] 已登录时显示用户信息
- [ ] 用户头像正确显示（有/无）
- [ ] 手机号正确脱敏（138****8000）
- [ ] 点击"退出登录"显示确认弹窗
- [ ] 确认退出后清除登录状态
- [ ] 退出后返回游客界面

**依赖**: Task 1（User实体修复）

---

## 🟡 Phase 2: Medium Issues (建议修复)

### Task 4: 实现游客数据迁移逻辑

**优先级**: P1
**预计时间**: 6小时
**状态**: ⏳ 等待Phase 1完成

**问题描述**:
用户登录后无法绑定���客模式下的学习数据。

**影响文件**:
- `lib/presentation/controllers/auth_controller.dart`
- `lib/data/repositories/auth_repository_impl.dart`
- `lib/domain/repositories/i_auth_repository.dart`
- `doc/api/user.md` (需要新增API文档)

**修改详情**:
（详细步骤在Phase 1完成后展开）

**依赖**: Phase 1所有任务

---

### Task 5: 实现Token自动刷新

**优先级**: P1
**预计时间**: 3小时
**状态**: ⏳ 等待Phase 1完成

**问题描述**:
Token过期后用户会被强制登出，体验不佳。

**影响文件**:
- `lib/presentation/controllers/auth_controller.dart`

**修改详情**:
（详细步骤在Phase 1完成后展开）

**依赖**: Phase 1所有任务

---

## 🟢 Phase 3: Low Issues (可延后)

### Task 6: 注册页国际化

**优先级**: P2
**预计时间**: 1小时
**状态**: ⏳ 可延后到v1.1

### Task 7: 429错误特殊提示

**优先级**: P2
**预计时间**: 30分钟
**状态**: ⏳ 可延后到v1.1

---

## 📅 实施时间表

### Day 1（预计6.5小时）
- 09:00-11:00: Task 1（User数据模型修复）
- 11:00-11:30: Task 2（密码加密提前）
- 13:00-17:00: Task 3（ProfileTab实现）
- 17:00-18:00: 集成测试 + 文档更新

### Day 2（可选，Phase 2）
- 09:00-12:00: Task 4（游客数据迁移）
- 13:00-16:00: Task 5（Token自动刷新）
- 16:00-18:00: 全面测试

---

## 🧪 测试清单

### 功能测试
- [ ] 用户注册成功
- [ ] 用户登录成功
- [ ] 游客模式正常使用
- [ ] "我的"Tab未登录状态正确
- [ ] "我的"Tab已登录状态正确
- [ ] 退出登录功能正常
- [ ] 用户信息显示正确
- [ ] 头像显示正常（有/无）

### 安全性测试
- [ ] 密码在Controller层立即加密
- [ ] 网络请求中密码为SHA256值
- [ ] Token安全存储

### 数据一致性测试
- [ ] API响应正确解析为User对象
- [ ] User字段与API文档一致
- [ ] 没有多余字段（uid, email, roleId）

### UI测试
- [ ] 横屏布局正常
- [ ] iPad和手机布局适配
- [ ] Liquid Glass设计风格一致

---

## 📊 进度追踪

| Task | 状态 | 开始时间 | 完成时间 | Code Review | 备注 |
|------|------|---------|---------|-------------|------|
| Task 1 | 🔴 未开始 | - | - | - | - |
| Task 2 | 🔴 未开始 | - | - | - | - |
| Task 3 | 🔴 未开始 | - | - | - | - |
| Task 4 | ⏳ 等待 | - | - | - | - |
| Task 5 | ⏳ 等待 | - | - | - | - |
| Task 6 | ⏳ 延后 | - | - | - | - |
| Task 7 | ⏳ 延后 | - | - | - | - |

---

## 🎯 成功标准

Phase 1完成后，应满足：
1. ✅ User数据模型与API文档完全一致
2. ✅ 密码在内存中的明文停留时间最短
3. ✅ 用户可以在App内退出登录
4. ✅ 所有代码通过code review
5. ✅ 没有引入新的bug
6. ✅ 测试清单100%通过

---

**下一步**: 准备开始Task 1 - 修复User数据模型
