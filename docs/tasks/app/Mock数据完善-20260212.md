# Mock数据完善-20260212

**任务ID**: TASK-002
**创建日期**: 2026-02-12
**优先级**: 🔴 P0 (高优先级)
**预计时间**: 2小时
**负责人**: AI
**状态**: 📝 待办

---

## 📋 任务概述

完善 Mock 数据系统，为所有场景和场景物品添加 dataFile 字段，支持完整的数据流，并实现 Repository 双模式（Mock/API）切换。

## 🎯 任务目标

1. 为所有 Scene 实体添加 dataFile 字段
2. 为所有 SceneItem 实体添加 dataFile 字段
3. 创建至少 3 个场景的完整互动数据 JSON 文件
4. 实现 Repository 双模式支持（Mock/API）
5. 添加环境变量控制数据源切换

---

## 📝 功能拆解

### 子任务 1: 更新数据模型
**预计时间**: 30分钟

**需要修改的文件**:
- `lib/domain/entities/scene.dart`
- `lib/domain/entities/scene_item.dart`

**具体内容**:
```dart
// scene.dart
class Scene {
  final String id;
  final String name;
  final String description;
  final String imageUrl;
  final String? dataFile;  // 新增字段
  // ... 其他字段
}

// scene_item.dart
class SceneItem {
  final String id;
  final String name;
  final String nameEnglish;
  final String? dataFile;  // 新增字段
  // ... 其他字段
}
```

**验收标准**:
- [ ] Scene 类添加 dataFile 可选字段
- [ ] SceneItem 类添加 dataFile 可选字段
- [ ] 所有现有代码编译通过

---

### 子任务 2: 更新 Mock 数据
**预计时间**: 30分钟

**需要修改的文件**:
- `lib/data/mock/mock_scenes.dart`

**具体内容**:
为所有 Scene 和 SceneItem 添加 dataFile 字段值

```dart
Scene(
  id: 'daily_breakfast',
  name: '早餐时间',
  dataFile: 'assets/data/scenes/daily_breakfast.json',  // 新增
  // ... 其他字段
)
```


**验收标准**:
- [ ] 所有 Scene 对象包含 dataFile 字段
- [ ] 所有 SceneItem 对象包含 dataFile 字段（如需要）
- [ ] dataFile 路径遵循统一命名规范

---

### 子任务 3: 创建场景互动数据 JSON 文件
**预计时间**: 45分钟

**需要创建的文件**:
- `assets/data/scenes/daily_breakfast.json`
- `assets/data/scenes/daily_school_prep.json`
- `assets/data/scenes/daily_cooking.json`

**JSON 数据结构**:
```json
{
  "sceneId": "daily_breakfast",
  "sceneName": "早餐时间",
  "sceneNameEnglish": "Breakfast Time",
  "imageUrl": "assets/images/scenes/daily_breakfast.png",
  "regions": [
    {
      "id": "item_001",
      "text": "牛奶",
      "textEnglish": "milk",
      "pinyin": "niú nǎi",
      "pronunciation": "niu2 nai3",
      "hotspot": {
        "type": "rect",
        "x": 100,
        "y": 150,
        "width": 80,
        "height": 120
      },
      "audioUrl": "assets/audio/milk.mp3",
      "character": "奶",
      "strokes": [...]
    }
  ]
}
```

**验收标准**:
- [ ] 创建至少 3 个场景的完整 JSON 文件
- [ ] 每个场景包含 10-12 个互动区域
- [ ] JSON 结构符合 InteractiveImageData 模型
- [ ] 所有必填字段完整

---

### 子任务 4: 实现 Repository 双模式
**预计时间**: 30分钟

**需要修改的文件**:
- `lib/data/repositories/scene_repository_impl.dart`
- `lib/core/config/app_config.dart` (新建)

**具体内容**:

1. 创建配置文件
```dart
// app_config.dart
class AppConfig {
  static const bool useMockData = bool.fromEnvironment(
    'USE_MOCK_DATA',
    defaultValue: true,
  );
}
```

2. 更新 Repository
```dart
class SceneRepositoryImpl implements SceneRepository {
  final ApiClient? _apiClient;

  @override
  Future<List<Scene>> getScenesByCategory(String categoryId) async {
    if (AppConfig.useMockData) {
      return _getMockScenes(categoryId);
    } else {
      return _getApiScenes(categoryId);
    }
  }

  Future<List<Scene>> _getMockScenes(String categoryId) async {
    // 现有 Mock 逻辑
  }

  Future<List<Scene>> _getApiScenes(String categoryId) async {
    // API 调用逻辑（待实现）
    throw UnimplementedError('API mode not implemented yet');
  }
}
```

**验收标准**:
- [ ] 创建 AppConfig 配置类
- [ ] Repository 支持 Mock/API 双模式
- [ ] 可通过环境变量切换模式
- [ ] Mock 模式正常工作
- [ ] API 模式抛出明确的未实现错误

---

## ✅ 验收标准

### 功能验收
1. [ ] 所有数据模型包含 dataFile 字段
2. [ ] 所有 Mock 数据包含 dataFile 值
3. [ ] 至少 3 个场景有完整的 JSON 数据文件
4. [ ] Repository 支持 Mock/API 双模式切换
5. [ ] 可通过环境变量控制数据源

### 代码质量
1. [ ] 代码编译无错误
2. [ ] 代码格式符合规范
3. [ ] 类型安全，无警告
4. [ ] JSON 文件格式正确

### 测试验收
1. [ ] Mock 模式下应用正常运行
2. [ ] 可以加载场景列表
3. [ ] 可以进入互动页面
4. [ ] JSON 数据正确解析

---

## 📦 依赖关系

**前置依赖**: 无

**后续任务**:
- TASK-003 ~ TASK-008: 日常生活场景实现（依赖本任务的 JSON 数据结构）
- TASK-009: TTS 功能完善（依赖本任务的音频路径）

---

## 📂 相关文件

### 需要修改的文件
- `lib/domain/entities/scene.dart`
- `lib/domain/entities/scene_item.dart`
- `lib/data/mock/mock_scenes.dart`
- `lib/data/repositories/scene_repository_impl.dart`

### 需要创建的文件
- `lib/core/config/app_config.dart`
- `assets/data/scenes/daily_breakfast.json`
- `assets/data/scenes/daily_school_prep.json`
- `assets/data/scenes/daily_cooking.json`

---

## 🔄 实施步骤

1. **第一步**: 更新数据模型（子任务1）
2. **第二步**: 更新 Mock 数据（子任务2）
3. **第三步**: 创建 JSON 文件（子任务3）
4. **第四步**: 实现双模式（子任务4）
5. **第五步**: 测试验证

---

## 📝 注意事项

1. **JSON 文件路径**: 确保 assets 目录在 pubspec.yaml 中正确配置
2. **数据一致性**: JSON 数据结构要与 InteractiveImageData 模型匹配
3. **向后兼容**: dataFile 字段设为可选，不影响现有代码
4. **命名规范**: 文件名使用小写+下划线格式

---

## 🎯 成功指标

- Mock 数据系统完整可用
- 数据流清晰：Scene → dataFile → JSON → InteractiveImageData
- 为后续场景开发提供标准模板
- Repository 架构支持未来 API 集成

---

**创建时间**: 2026-02-12
**最后更新**: 2026-02-12
