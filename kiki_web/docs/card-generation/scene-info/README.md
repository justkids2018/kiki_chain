# 场景资料统一目录（scene-info）

此目录用于统一管理学习卡片生产资料，按“场景 -> 卡片”组织。

## 目录规范

```text
scene-info/
  <scene_slug>/
    <card_slug>/
      <card_slug>.jpg|png|webp   # 场景图
      <card_slug>.json           # 热区数据（items_data）
      prompt.md                  # 生图提示词
```

## 命名规则

- 同一卡片的图片与 JSON 必须同名（仅扩展名不同）
- 推荐使用小写英文+下划线命名：`kiki_jiaoshi`
- `prompt.md` 固定文件名，便于脚本读取

## 当前示例

- `demo_animals/kiki_dongwuyuan/`
- `demo_plants/kiki_zhiwuyuan/`
- `demo_classroom/kiki_jiaoshi/`

## 当前规划分类

- `scene_01_晨光乐趣/`：校园晨间、阅读、手工、花园、教室角落、图书角、午餐、午睡、音乐律动、放学回家
- `scene_02_数学思维/`：数字、形状、计数、测量、配对、比较
- `scene_03_日常生活/`：超市、公园、生日、洗澡、穿衣、看医生
- `scene_04_游乐场景/`：恐龙、太空、海盗船、城堡、马戏团、沙滩
- `scene_05_中国节日/`：端午、中秋、重阳、冬至、春节、元宵节
- `scene_06_世界足球节/`：球场开赛、训练时间、观赛派对、颁奖时刻、球迷广场、守门练习、足球现场
- `scene_05_入学准备/`：（已弃用，保留作历史参考；详见目录内 README）

## 词条选取原则（3-6 岁友好）

- 词条以孩子能直接看见、摸到的具体物品为主，避免抽象概念（情绪/方位/动作/序数/序列）作为目标词。
- 汉字长度优先 2 字，允许 3 字（如「糖葫芦」「文具盒」），禁止出现 4 字成语或抽象组合作为目标词。
- 每张卡 8 个目标词须围绕同一场景天然聚合：节日卡建议 2-3 食物 + 2-3 标志物 + 1-2 活动道具 + 0-1 人物玩偶。
- 易混淆物品（如灯笼/花灯/龙灯、汤圆/月饼、贴纸/徽章）必须在卡内通过形状、颜色、位置三重区分。
- 先维护 `<card_slug>.md` 作为生图与后续 JSON 解析的语义源；图片、热区 JSON、音频可以按卡片逐张生成。

## 迁移建议

- 生产资料放在本目录维护
- Flutter 运行时资产（`assets/images` 和 `assets/data`）可由脚本从本目录同步生成
